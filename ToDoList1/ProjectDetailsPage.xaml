<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ToDoList1.ProjectDetailsPage"
             Title="Проект">

    <Grid RowDefinitions="Auto,*,Auto" RowSpacing="0" BackgroundColor="#f8f9fa">
        
        
        <Border x:Name="ProjectHeaderBorder"
                Grid.Row="0"
                Margin="0,0,0,15"
                Stroke="#dee2e6"
                StrokeThickness="1"
                BackgroundColor="White">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="15"/>
            </Border.StrokeShape>
            <Grid>
                <Grid x:Name="ViewModeGrid" IsVisible="True">
                    <VerticalStackLayout Padding="20">
                        <Label x:Name="ProjectTitleLabel"
                           FontSize="24"
                           FontAttributes="Bold"
                           TextColor="#2c3e50"/>
                        <Label x:Name="ProjectDescriptionLabel"
                           FontSize="16"
                           TextColor="#7f8c8d"
                           Margin="0,10,0,0"/>
                    </VerticalStackLayout>
                </Grid>

                <Grid x:Name="EditModeGrid" IsVisible="False" Padding="20">
                    <VerticalStackLayout Spacing="15">
                        <Label Text="Название проекта" FontSize="16" FontAttributes="Bold"/>
                        <Entry x:Name="EditNameEntry" Placeholder="Название" />

                        <Label Text="Описание" FontSize="16" FontAttributes="Bold"/>
                        <Editor x:Name="EditDescEditor" HeightRequest="50" />

                        <HorizontalStackLayout Spacing="10">
                            <Button Text="Сохранить" Clicked="SaveEdit_Clicked" BackgroundColor="#27ae60" TextColor="White" CornerRadius="10"/>
                            <Button Text="Отмена" Clicked="CancelEdit_Clicked" BackgroundColor="#95a5a6" TextColor="White" CornerRadius="10"/>
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                </Grid>
                <Button x:Name="EditButton"
                        Grid.Row="0"
                        HorizontalOptions="End"
                        VerticalOptions="Start"
                        Margin="0,10"
                        Text="✏️ Редактировать"
                        Clicked="EditButton_Clicked"
                        BackgroundColor="Transparent"
                        TextColor="#3498db"
                        FontSize="14"/>
            </Grid>
        </Border>

        <ListView x:Name="TasksList"
                  Grid.Row="1" HasUnevenRows="True">
            <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Margin="5" BackgroundColor="White">
                        <Grid ColumnDefinitions="Auto,*,Auto,Auto">
                            <CheckBox Grid.Column="0"
                                      VerticalOptions="Center"
                                      IsChecked="{Binding IsCompleted}"/>

                            <StackLayout Grid.Column="1" Margin="10,0">
                                <Label Text="{Binding Name}"
                                       FontSize="16"
                                       TextColor="Black"/>
                                <Label Text="{Binding Description}"
                                       FontSize="12"
                                       TextColor="Gray"
                                       MaxLines="2"/>
                                <Label Text="Выберите тег"/>
                                    <Picker ItemsSource="{Binding Path=BindingContext.Tags, Source={RelativeSource Mode=FindAncestor, AncestorType={x:Type ContentPage}}}"
                                            Title="Теги"
                                            ItemDisplayBinding="{Binding Name}"
                                            SelectedItem="{Binding Tag}"/>
                                </StackLayout>

                            <Button Grid.Column="2"
                                    Text="✏️"
                                    BackgroundColor="Transparent"
                                    Clicked="EditTask_Clicked"
                                    CommandParameter="{Binding .}"/>
                            <Button Grid.Column="3"
                                    Text="❌"
                                    BackgroundColor="Transparent"
                                    Clicked="DeleteTask_Clicked"
                                    CommandParameter="{Binding .}"/>
                            </Grid>
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Text="+ Задача"
                Grid.Row="2"
                BackgroundColor="#3498db"
                TextColor="White"
                FontSize="16"
                CornerRadius="25"
                HeightRequest="50"
                Clicked="AddTask_Clicked"/>

        <StackLayout x:Name="PodTasksContainer"
                     IsVisible="False"
                     Padding="20,5"
                     BackgroundColor="#f8f9fa">
            <Label Text="Подзадачи:"
                            FontSize="14"
                            FontAttributes="Bold"
                            Margin="0, 0, 0, 5"/>

            <StackLayout Grid.Row="1" Padding="20,5" BackgroundColor="#f8f9fa">
                <CollectionView ItemsSource="{Binding PodTasks}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <CheckBox Grid.Column="0"
                                    IsChecked="{Binding IsCompleted}"
                                    VerticalOptions="Center"/>

                                <Label Grid.Column="1"
                                    Text="{Binding Title}"
                                    Margin="10, 0"
                                    VerticalOptions="Center"/>

                                <Button Grid.Column="2"
                                    Text="🗑️"
                                    BackgroundColor="Transparent"
                                    FontSize="10"
                                    Clicked="DeletePodTask_Clicked"
                                    CommandParameter="{Binding .}"/>
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </StackLayout>

            <Grid ColumnDefinitions="*, Auto" Margin="0, 10, 0, 0">
                <Entry x:Name="PodTaskEntry"
                      Placeholder="Новая подзадача"
                      Grid.Column="0"/>
                <Button Text="+"
                        Grid.Column="1"
                        BackgroundColor="#3498db"
                        TextColor="White"
                        Clicked="AddPodTask_Clicked"
                        CommandParameter="{Binding .}"/>
            </Grid>
        </StackLayout>

        <StackLayout Orientation="Horizontal"
                         Grid.Row="3"
                         Padding="20"
                         Spacing="10">
        </StackLayout>
    </Grid>
</ContentPage>